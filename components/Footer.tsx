import { ReactElement } from "react";
import styled, { StyledComponent } from "styled-components";

const Footer: StyledComponent<"section", any, {}, never> = styled.footer``;

export default (): ReactElement =>
    <Footer></Footer>